const fs = require(" fs\); let code = fs.readFileSync(\src/App.jsx\, \utf8\); code = code.replace(/function App\\(\\) \\{/, const getAcceptanceColor = (duration, std) => {\n const dur = parseFloat(duration) || 0;\n const standard = parseFloat(std) || 1;\n const ratio = dur / standard;\n if (ratio > 1.0) return \rgba 239 68 68 0.4 \;\n if (ratio > 0.8) return \rgba 234 179 8 0.4 \;\n return \rgba 34 197 94 0.4 \;\n};\n\nfunction App() {); fs.writeFileSync(\src/App.jsx\, code);